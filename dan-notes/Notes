cpumaps
bit map of real CPUS in 8-bit bytes (IN)
    * each set to 1 means that corresnponding CPU is usable
    * In each byte, lowest CPU number is least significant bit 
    * Bytes are stored in little endian order: CPU0-7, 8-15